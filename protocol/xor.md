# XOR 决策协议（XOR Protocol）

## 定义

当系统进入以下状态：

- A 成立
- B 成立
- A 与 B 互斥

系统无法通过传统逻辑求解。

---

## 处理机制

χ(A, B) = I(A, B, S)

其中：

- S：当前主体状态
- I：主体干预函数

---

## 结果

系统不会停止，而是强制产生单一选择：

- A 或 B（由主体状态决定）

---

## 本质

XOR 不是逻辑运算，而是主体生成事件。
