# 理论主体（Theory Core）

## 遞归主体模型

S(t) = f(S(t), E(t))

主体不是静态存在，而是自我运行的系统。

---

## XOR 决策机制

χ(A, B) = I(A, B, S)

当逻辑冲突发生时，系统进入不可还原决策状态。

---

## 热补丁更新机制

S(t+1) = S(t) + ΔS（运行中更新）

系统不依赖停机维护，而是在执行中修正自身。
